# 教訓

壊れ方の記録。なぜその設計にしたかは `docs/decisions/` にある。ここに書くのは、何が静かに嘘をついたか。

各行に 3 列を置く。何が起きたか、何で気づいたか、いま何が守っているか。**3 列目が「未対応」の行は教訓ではなく穴。**その数を減らすのが Phase 8 の仕事のひとつ。

3 列目に挙げた検査は `npm test` で毎回走る。1 件でも落ちたら、そこに書いた壊れ方が戻ってきている。

## 検査が守っているもの

| 何が起きたか | 何で気づいたか | いま何が守っているか |
|---|---|---|
| 基準画像の枚数が 4 通りの古い値で散っていた。`docs/hud.md` が 34 と 19、`src/hud/hud.ts` が 37、`tests/e2e/smoke.spec.ts` が 39。実際は 42 | Phase 8 の着手前調査 | `tests/tools/scenes.test.ts` が総数・HUD 入り・空母・ミッションの枚数を固定する |
| 画素比較の道具と基準画像を撮る側で雲量の既定が違った。前者は 0.3、後者は 0。全カットが明示していたので露見していなかった | 同上 | `tests/e2e/scenes.mjs` の `captureParams` を両方が呼ぶ。`tests/tools/scenes.test.ts` が既定値を固定 |
| ブラウザの起動引数を 2 か所に写しで持っていた。1 つ違うだけでラスタライザが変わる | 同上 | `tests/e2e/launch.mjs` を `playwright.config.ts` と `tools/exact.mjs` の両方が読む |
| `docs/flight-model.md` の誘導抗力係数が `0.1158` のままだった。AR 3.44（F-16）の値で、実装は 0.113173 | 同上 | `tests/tools/docsNumbers.test.ts` がドキュメントの数値と実装を突き合わせる |
| 翼面荷重を 4,395 N/m² と書いていた。実測は 4,393.99 で 4,394 | `tests/tools/docsNumbers.test.ts` を書いた直後に落ちた | 同上。ドキュメントから読んだ値と計算値を突き合わせ、期待値を 2 度書かない |
| `TestHook` に項目を足したのに `publish` の代入だけ抜け、E2E が「ミサイル 0 発」と報告した | E2E の数値検査 | `tests/render/testHook.test.ts` が全項目を機械的に検査する |
| `src/sim` から three を import しても、実行するまで気づけない | Phase 0 の設計 | `tests/sim/layering.test.ts` が禁止 13 パターンを検査し、既知違反 12 件で発火も確かめる |
| 操作説明と実装のキー割り当てがずれる | Phase 7 | `tests/input/controlHelp.test.ts` が `poll()` のソースと `CONTROL_HELP` を双方向に突き合わせる |
| ポーズ（Escape）が `CONTROL_HELP` に載っていない。判定が `main.ts` にあって抽出対象の外、しかも `event.key` を使うので正規表現にも当たらなかった。**二重に外れていた** | Phase 8 の着手前調査 | 同じ検査が `src/` からキーボードを見ているファイルを探して全部読む。`event.key` での判定も禁じる |
| 符号と向きだけを見ていると、掛け算を割り算に変えても落ちない。`assist.ts` の自動水平化は `-bank * 1.2` を `-bank / 1.2` にしても全テストが通った | Stryker の掃引（段 4） | `tests/sim/assist.test.ts` がゲインの大きさと境界を固定する。歯型 `level-gain-divide` `recover-pitch-boundary` |
| 基準画像 42 枚が VFX を見張っていなかった。宣言 56 件のうち発火したのは 27 件で、大面積のもの（`aircraft` 13、`terrain` 9、`water` 4）だけ。`maxDiffPixelRatio: 0.005` は 4,608 画素まで許すが、フレアの寄与は 1,590、曳光弾 304、爆発 52 画素 | 段 6 の画素の逆テスト | `tests/e2e/pixel-mutate.spec.ts`（`MUTATE=1`）が宣言と実測を突き合わせる。許容を 0 にした |
| 逆テストの基準画像が別ディレクトリに解決され、比較ではなく新規作成になっていた。`toHaveScreenshot` は spec ファイル名から置き場を決める。**落ちるはずの検査が静かに通る** | 書いた直後に気づいた | `playwright.config.ts` の `snapshotPathTemplate` が置き場を spec 名から切り離す |
| 追い越しの判定と DLZ の clamp が丸ごと素通りしていた。`overtook = true` を `false` にしても、`rNe > rMax` の補正を消しても落ちない | 同上 | `tests/sim/dlz.test.ts` が値を固定する。歯型 `dlz-overtook-never` `dlz-drop-clamp` |
| `webglVersion` をバックエンドの facade へ寄せるとき、値を `kind` から導いた。`kind === 'node-webgpu' ? 0 : 2` は WebGL 経路で必ず 2 を返すので、`smoke.spec.ts` の「WebGL2 が取れているか」が**原理的に落ちなくなっていた**。継ぎ目を作る作業がそのまま見張りを外す | 段 7 の差分を読み直したとき | `RenderBackend.webglVersion` が `gl.VERSION` の実物を読む。`smoke.spec.ts` は `hook.backend` も見る。node 側は `backend.gl` の実物から読み、`tests/render/nodeBackend.test.ts` が「`kind` が node-webgl でもコンテキストが無ければ 0」と「WebGL1 なら 1」で言い換えへの後戻りを縛る。排出できるかも `kind` ではなく `gl` の有無で決める。歯型 `node-backend-version-from-kind` と `node-backend-drain-from-kind` |
| 継ぎ目は、越える者が現れた瞬間に意味を失う。帳簿からレンダラを 1 行借りても絵は動かないので、基準画像 42 枚は気づかない。**段 15 で実装を差し替えたときに初めて落ちる** | 段 8 で継ぎ目を切ったとき | `tests/render/pipelineSeam.test.ts` が `scene.ts` の本文と import を検査する。`createGpuTimer` を 1 行足すと 2 件落ちることを実測した |
| three の描画順は、不透明が `material.id` を深度より先に見る（`WebGLRenderLists.js:1-49`）。`material.id` も `object.id` も生成のたびに 1 増える大域の連番なので、**組み立ての順番を入れ替えると前後関係に関係なく絵が動く** | 段 8 で 1,130 行を移す前に `node_modules` を読んだ | 組み立ての順番は `pipeline/webgl.ts` の 1 か所だけ。`npm run exact` が許容 0 で 42 枚を数える |
| node 経路の `ShaderMaterial` は**落ちない**。`THREE.NodeBuilder: Material "ShaderMaterial" is not compatible.` をコンソールへ出したまま描画が進み、`initError` も立たない。例外で止まると思っていると、エラーを 1 行見落とした時点で「動いている」と読む | 段 9 で実際に 1 枚入れて測った | `tests/e2e/node-path.spec.ts` が `shaderMaterials` の数と `errors` の両方を見る。1 枚入れると 2 件落ちることを実測した |
| node 経路の `info` は自分で 0 に戻さないと積算される。`autoReset` は既定 true だが、`info.reset()` は `Animation.js:75`（`setAnimationLoop` の中）からしか呼ばれない。`WebGLRenderer` は `render()` の中で戻すので、作法が逆になる | 9 枚描いたらドローコール 52 が 468、三角形 18,899 が 170,091 とちょうど 9 倍になった | `src/render/pipeline/node.ts` が読む直前に `info.reset()` を呼ぶ。段 15 で `backend.resetInfo()` を node 側へ移すときの前提 |
| ライブラリの peer に上限が無いと、**「対応した」と「いま噛み合う」の差に気づけない。**takram 0.19.1 の peer は `three: '>=0.170.0'` だが、three 0.185 で `struct()` の戻り値が Proxy になり、takram が読む `.layout` が消えた。モジュール評価の時点で落ちる | 段 10 で `?gpu=2` を立てたとき | three を 0.184 に留め、`tests/render/atmosphereCompat.test.ts` が `struct()` の形を単体テストで縛る。three を上げた瞬間に落ちる |
| 三者の噛み合わせは、**プロトタイプに生やしても越えられないことがある。**`StructTypeNode.prototype.layout` を足すと 1 つ目の壁は越えたが、geospatial は `'layout' in s` で存在そのものを見ており、Proxy に `has` トラップが無いので false のままだった | shim を入れて測り直したとき | 版で解いた。`docs/decisions/0010-webgpu-tsl.md` に両方の壁を書いた |
| **`forceWebGL: true` は退避路にならなかった。**計画は「TSL で 1 度書けば両方で動く」と書いていたが、node 経路の WebGL2 バックエンドは大気の構造体を GLSL へ落とせない（`'AtmosphereParameters' : syntax error`） | 段 10 で `?gpu=1` に大気を入れて測った | `tests/e2e/node-path.spec.ts` が `?gpu=1` で `atmosphere === false` を見る。大気から先は `?gpu=2` だけ |
| 読み戻しの向きと原点がバックエンドで違う。WebGL2 は行が下から・原点が左下、WebGPU は行が上から・原点が左上。しかも WebGPU は行を 256 バイトへ揃える（16 px 幅を 16 行で 3,904 バイト）。**算術は合っていて並びだけが違う**ので、気づかないと「WGSL がずれている」と読み違える | 段 11 でハッシュの格子の先頭が `hashTopByte(0, 15, 0)` に一致して分かった | `src/render/pipeline/readback.ts` の `unpadRows` が詰め物と並びを揃える（`tests/render/readback.test.ts` が単体で縛る）。`tests/e2e/node-path.spec.ts` が 768 + 1,024 個をビットで突き合わせる |
| TSL の `toVar()` と `assign()` は `Fn` の中にしか置けない。素の関数で使うと `THREE.TSL: No stack defined for assign operation.` が出る | 段 11 で `worley` を素の関数として書いて踏んだ | `worley` と `perlin` を `Fn(() => ...)()` で包んだ。`tests/e2e/node-path.spec.ts` がノイズの 1,024 個を突き合わせるので、壊れれば落ちる |
| `--update-snapshots` の既定は `changed` で、**比較器が「変わっていない」と判断した画像は書き直さない。**`threshold: 0.05` は 12.75 階調まで同一とみなすので、1〜8 階調の変化は許容の内側に入り、撮り直したのに古いバイトが残る。段 6 で許容を 0 にした関門と食い違う | 雲の上限距離を縮めて撮り直した直後、`npm run exact` が 3 枚を「動いた」と報告した | `npm run exact` が許容 0 で数える。撮り直しは `--update-snapshots=all` で行う |
| 雲のマーチの上限距離だけ縮めても費用は下がらない。`stepGrowthScale` が「その歩数で上限距離を覆う」ように歩幅を解くので、同じ歩数で細かく刻み直すだけになる。密度サンプルは 29.2 から 38.8 へ**増えた** | 26 km を 12 km にして測ったとき | `tests/render/quality.test.ts` が「歩数が上限距離を覆い切る」ことと「上の 2 段は上限でも手前の 4 倍より細かい」ことを縛る |
| 雲量の応答が 0.3 付近で急峻。`threshold = 1 - coverage` を気象マップの FBM へ当てているので、0.3 は分布の裾に載っている。0.02 下げると主役以外の雲が消え、0.24 より下は画面から雲が消えて密度サンプルが 3 点とも同値になる。**「少し下げる」に使える刻みは 0.01 しかない** | 0.30 から 0.16 まで振って測った | `docs/measuring.md` に応答の表を残した。既定は `pipeline/types.ts` の 1 か所 |
| **3D のレンダーターゲットから直に層を読み戻せない。**`readRenderTargetPixelsAsync` の `faceIndex` は node 経路の WebGL2 バックエンドで効かず、全部 0 が返る。`WebGLTextureUtils.copyTextureToBuffer` がキューブ以外を `gl.TEXTURE_2D` 決め打ちで `framebufferTexture2D` へ渡すため、アタッチが `GL_INVALID_OPERATION` で失敗し framebuffer が incomplete になる。**例外は飛ばず GL の警告が出るだけ。**焼き込みのほうは効いている | 段 12 で 64³ の層 0・32・63 を両バックエンドで測り直した。前回は「WebGPU でも同じ」と読んでいたが、WebGPU では効く | `src/render/clouds/volume.ts` の `readVolumeSlice` が整数フェッチで 2D へ落として読む。両バックエンドで効くことを実測した。`tests/e2e/node-path.spec.ts` が GLSL 版と 1,024 個をビットで突き合わせる |
| **node 経路はレンダーターゲットのテクスチャを v 反転して引く。**`TextureNode.setupUV()` が `builder.isFlipY()` のときに入れる補正で、`GLSLNodeBuilder` は true、`WGSLNodeBuilder` は false。両方合わせて「v = 0 は描いたときの NDC の上」に揃う。既定の `WebGLRenderer` の経路は逆なので、**同じ式を同じ uv で焼くと密度は上下が逆のノイズを引く** | 段 12 でノイズの体積が GLSL 版とビット一致しなかったとき。`uv` をそのまま焼いて読み戻すと、体積は (0.008, 0.992)、平面は (0.008, 0.008) を返した | 焼く側で打ち消す（`src/render/clouds/noiseNodes.ts` の `bakeUv`）。焼き直せないもの（マーチの出力）は読む側で裏返す（`src/render/clouds/resolveNodes.ts` の `sampleTarget`）。`tests/e2e/node-path.spec.ts` が形状ノイズ 1,024 個をビットで突き合わせ、足し込みの生バイトも 1 バイトも違わないことを見る |
| **分布は配置を見ない。**雲影 256² の 16 ビンのヒストグラムは、ノイズの体積を上下反転しても気象マップを上下反転しても、合格条件の 0.01 の内側に収まる。段 12 の合格条件はそれだけでは足りなかった | 気象マップだけ素の `uv()` へ戻して実測した。分布は通り、区画ごとの平均は 0.33 開いた | `shadowTileMeans` が 4x4 の区画平均を出し、`tests/e2e/node-path.spec.ts` が最大のずれ 0.02 未満を見る。**ノイズの反転はこの検査でも捕まらない**ので、ビット一致の検査が別に要る |
| **`Fn` の返り値を使わないと本体そのものが生成されない。**GLSL の `inout int samples` は `setLayout` を付けない `Fn` で写せるが、呼び出しの結果を捨てると加算が消える。例外も警告も出ない | 段 13 の前に小さなプローブで測ったとき。ループの回数（返り値を使う側）は CPU 参照と一致したのに、`Fn` の中で足した counter だけが 0 のまま返った | `lightOpticalDepth` は光学的厚みを返すので成立する。`tests/e2e/node-path.spec.ts` が密度サンプル数を整数で突き合わせるので、消えれば落ちる |
| **`screenCoordinate.y` は上から数える。**`gl_FragCoord.y` は下から。両バックエンドとも上からで一貫しているが、既定の経路とは逆になる。放置すると Bayer のディザが上下反転し、**開始位置のずれ方が変わるので歩数まで変わる** | 段 13 の前のプローブで、16 行の的の行 0 が `screenY = 15` を返した | `screenSize.y - 1 - y` で揃える。`tests/e2e/node-path.spec.ts` が密度サンプル数を整数で突き合わせる。反転を外すと落ちることを実測した |
| **通っていない枝は検査されない。**マーチの突き合わせを最初に置いた構図（雲底の下から見上げる、雲量 0.29）は、区画平均 16 個のうち 14 個が 0.000 で、歩数を使い切った画素が 0 だった。打ち切りの移植を一度も通さずに「一致した」と読むところだった | 数が一致したあとに区画平均と打ち切りの数を出して見た | 雲層の内側から水平に見る構図へ変え、歩数を 64 にした。`tests/e2e/node-path.spec.ts` が「全画素が歩いている」「打ち切りが 10 画素より多く、半数より少ない」「どの区画にも雲がある」を下限で縛る |
| **焼いた絵を近傍で舐めるとき、端の外をどう読むかが揃っていないと縁だけ値が変わる。**node 経路の的を作る道具が無条件に `RepeatWrapping` を設定していた。既定の経路の `WebGLRenderTarget` は端で止める | 段 13 の後半で足し込みを突き合わせたとき。36,864 バイト中 432 個が違い、最大差は 69 だった。**縁の画素の数と桁が合っていた** | `bakePlane` の折り返しは既定で切り、世界座標で引き回す気象マップだけが頼む。`tests/e2e/node-path.spec.ts` が足し込みの生バイトを 1 バイトも違わないことで縛る |
| **宣言されて毎フレーム代入されているのに、シェーダ本文が一度も読んでいない uniform があった。**`cloudResolve.frag` の `previousCameraPosition` | 段 13 の後半で移植する前に本文を読んだとき | 落とした。`tests/render/densityConstants.test.ts` が「戻していないこと」を見る |
| **GPU は float32 で解くので、計画が置いた 1e-3 m の合格条件には届かない。**高さ場の突き合わせは最大 4.5 mm ずれた。CPU 側の式を `Math.fround` で float32 に丸めるだけで 1.03 mm ずれるので、残りは丸めで説明が付く | 段 14 で高さ場を `src/sim/terrain.ts` と突き合わせたとき | 関門を 1 cm にした。**移植を間違えれば m の単位で外れる。**実測で、双三次の係数 5 を 4.99 にすると 14.18 m、タップの添字を 1 つずらすと 3.20 m、縁で止めるのをやめると 340.16 m。`tests/e2e/node-path.spec.ts` |
| **影マップの焼き込みの回数をドローコールで数えると当てにならない。**視錐台の切り方が主カメラと影のカメラで違うので、機体の 31 回に対して影のパスは 47 回だった。メッシュの枚数（33）とも一致しない（1 枚が複数の材質を持つと 1 回では描かれない） | 段 15 で「影マップは 1 回しか焼かれない」を確かめようとしたとき | `info.render.frameCalls` でパスの数を数える。`tests/e2e/node-path.spec.ts` |
| **影を有効にするだけで、投げ手が無くてもパスが 1 つ増える（WebGPU 経路）。**パスの絶対数や「無効との差」で影マップの回数を数えると、1 回しか焼いていないのに 2 回と読む | 投げ手を切ったまま有効にして測り直した。1 → 2 → 3（無効・投げ手なし・投げ手あり） | 回数は「投げ手あり」と「投げ手なし」の差で数える。増える 1 つの正体は未特定で、値を固定して変化に気づけるようにしてある。`tests/e2e/node-path.spec.ts` |
| 同じ GLSL の断片が `explosions.ts` と `flares.ts` に写しで置いてあった。**写しが 2 つあると、TSL へ移すときに突き合わせる相手が定まらない** | 段 16 で移す前に両方を読んだとき | `radialSprite.ts` を正本にして両方が読む。`explosions.ts` 側は `#ifdef` を立てないので振る舞いは変わらない（基準画像 42 枚で確かめた） |
| 既定の雲量を `capture.ts` と `pipeline/types.ts` に写しで持っていた。以前も道具ごとに違う雲量で撮った事故がある | 雲量を下げようとして 2 か所見つけた | 定義を 1 か所にし、`tests/tools/scenes.test.ts` が基準画像の雲ありのカットと本番の既定が揃っていることを縛る |
| **既定を切り替えると、既定を前提に決めた値がすべて棚卸しの対象になる。**段 20b で `DEFAULT_BACKEND` を `node` にしたら 9 種類が意味を失った。待ちの上限、比で縛った閾値、project の並び、GLSL 版を参照する 15 本の URL、ワーカー数。**どれも移植の欠陥ではない** | 撮り直したあとの通しで 15 件・5 件・1 件と 3 波に分かれて落ちた | 経路を指す `?path=` を足すのは `harness.ts` の 2 か所だけ。project ごとの起動引数は `launch.mjs` の `argsForProject` 1 か所。閾値は `onNodePath()` で経路ごとに分ける。戻す手順は 2 か所に絞ってある（ADR 0010 の「戻し方」） |
| **事前コンパイルの検査が node 経路で素通りしていた。**判定は `programs > 60` で、事前コンパイルを切ると 67 になる。入れると 61。**切ったほうが多いので、切っても通る。**旧経路は 25 対 119 で効いている | 段 20b で 4 条件を同じ harness で測ったとき。降格の側を測る前に起動の側を測り直した | node 経路の判定は `compileMs > 0` と暖機の所要。所要のほうは `node-path.spec.ts` の `RenderPipeline でポストの鎖が立つ` が持つ。`programs` の数は旧経路だけが見る |
| **0 を `mapSize` へ渡すと影マップが作られない。**`low` の `aircraftShadowMapSize` は 0 で、node 経路の `applyPreset` はそれを無条件に `shadow.mapSize.set(0, 0)` へ渡していた。旧経路の `aircraftShadow.ts` は `Math.max(1, size)` の下限、`size > 0` の分岐、`shadow.map` の破棄と作り直しを持っていたが、**移植でその 3 つとも落ちていた。**大きさを変えても作り直さないので、落ちない側の降格でも解像度が効いていない | 段 20c で降格を追いかけたとき。プリセット表を読んで 0 を見つけた | `nodeShadow.ts` の `setQuality` が下限と作り直しを持ち、`tests/render/nodeShadow.test.ts` が両方を縛る。歯型 `node-shadow-zero-mapsize` と `node-shadow-no-recreate` |
| **切り分けの道具そのものが壊れていると、候補を誤って外す。**`?env=0` で環境反射を切ったつもりで測り、再現したので環境マップを候補から外した。実際は `applyPreset` が降格のたびに `environmentNode` を無条件で上書きしており、**最初の降格で環境反射が戻っていた。**起動時の指定を実行時の処理が踏み潰す形 | 候補を外した後、`applyPreset` を読み直したとき | `applyPreset` は大きさが変わったときだけ作り直し、`options.showEnvironment === false` を尊重する。**機械の検査は無い**（`applyPreset` は実物の WebGPU レンダラを要るので単体で縛れない）。切り分けの前に道具そのものを確かめる、が残る教訓 |
| **降格に頼るプローブは、降格が届いたかを出力しないと嘘をつく。**`medium → low` で落ちる欠陥を追うのに自動降格で測っていたが、降格は 1 段ずつなので 4,148 フレーム通った条件は medium で止まっていた可能性がある。**9 手ぶんの切り分けが根拠として無効だった** | 設定画面から品質を切り替える形に変えたら、`low` への遷移だけが落ちると一手で出た | `品質を 4 段切り替えても描画が続く`（`smoke.spec.ts`）が 4 段すべての遷移を必ず通る。降格の時機に依存しない。**旧経路で緑・node 経路で赤を両方測ってから入れた** |
| **当て推量は終わらない。差分を二分すれば終わる。**`low` だけが落ちる原因を、`cloudDetail`・`cloudGroundShadow`・`skyEnvironmentSize`・VFX の 5 列と個別に当てて外し続けた。`low` と `medium` の差は 23 列で、**二分なら 5 手で 1 つに絞れる**（実測 4 手で `cloudResolutionScale`） | 2 回外したところで、候補の数を数えた | `getQuality` に列を差し替える一時処理を入れて振る。**候補が数えられるなら二分を選ぶ** |
| **URL を自前で組むヘルパは経路を取りこぼす。**`smoke.spec.ts` の設定画面の群の `openSettings` が `?gpu=3` を足しておらず、node 経路の project で回しても旧経路を見て通っていた。検査 7 件が空振り。段 20a-3 で同じ形を記録したのに、記録した当人のコードに残っていた | 段 20c で 4 段切り替えの検査を足すとき、既存のヘルパを読んだ | `openSettings` が `onNodePath()` を見る。**経路を足すのは `harness.ts` の `capture` と `openLive`、それにこのヘルパの 3 か所だけ** |
| **キャプチャモードは 1 枚で止まるので、ライブループを回す経路を検査が持っていなかった。**段 20b で既定を node 経路へ切り替えたところ、遊んでいると約 450 フレームで描画ループごと止まった。品質の自動降格が引き金で、`clouds.setQuality` が組み直した材質を最初に描く `createBindGroup` が `Cannot read properties of undefined (reading 'mipLevelCount')` で落ちる。**E2E 277 件は全部緑のまま公開まで抜けた** | 公開ページで遊んだユーザの指摘。こちらの検査は 1 件も落ちていなかった | `品質を 4 段切り替えても描画が続く`（`smoke.spec.ts` の設定画面の群）が 4 段すべての遷移を通り、各段でシムのフレームが進むことと例外 0 件を見る。**旧経路で緑（52.7 秒）・node 経路で赤を両方測ってから入れた。**落ちる列は `cloudResolutionScale` まで絞れているが、**機構は未特定** |
| **`RenderTarget.setSize()` は `dispose()` を呼ぶのに `texture.version` を上げない。**three の束縛は版が変わったときだけ GPU テクスチャを作り直す（`Sampler.update()` → `Bindings.js`）。版が据え置きのまま実体だけ消えるので、`WebGPUBindingUtils.createBindGroup` が `textureData.texture` を `undefined` のまま読んで `mipLevelCount` で落ち、**描画ループごと死ぬ。**毎フレーム描き先になる的は作り直されるので当たらない。**「読まれるのが先、書かれるのが後」の的だけ**が穴に落ちる（雲の `history`） | 段 20c で `medium → low` の遷移を二分し、列を絞ってから three の実装を読んだ | 張り替えたら次の描画で解決より前に 1 度 `history` を描き先にする（`cloudsNodePass.ts` の `historyNeedsFill`）。`品質を 4 段切り替えても描画が続く` が赤から緑へ変わることで確かめた |
| **検査そのものが空振りしていないかを別の値で確かめる。**「長く飛んでも描画が止まらない」を書いたが、`KeyW` はピッチ**下げ**なので放っておくと海面へ墜ち、シムが止まって**検査が通ってしまう。**切り分けの最中に 2 回踏んだ形をそのまま検査に持ち込みかけた | 検査を書いたあと、飛んだ結果の高度を出してみた | `1,200 フレーム飛んでも描画が止まらない` が高度 300 m を下回っていたら落とす。**「通っていない枝は検査されない」の変種**で、検査が意図した状態を通ったかを別の値で見る |

## まだ守るものがない穴

| 何が起きたか | 何で気づいたか | いま何が守っているか |
|---|---|---|
| ADR 0001 が「対応した時点で再検討する」と条件を書いたが、条件が満たされたかを誰も確かめなかった。ライブラリは 9 か月前から対応済み | Phase 8 の着手前調査 | 未対応。段 1 の歯型表で扱う |
| `HudArmament.flares` は代入されているのに読む場所が 1 つもない。フレア残数が画面に出ない | 同上 | 未対応。段 30 で埋める |
| glb の `cockpit` に内装がないのに、ADR 0005 が「見せれば足りる」と書いていた。実測 244 三角形、0.43 × 0.41 × 0.96 m | 同上 | 未対応。ADR 0005 に実測を書いたのみ |
| WebGPU 経路で `shadowMap.enabled` を立てると、投げ手が無くても描くパスが 1 つ増える。影マップではない何かが足されている | 段 15 で投げ手を切って測ったとき | 未対応。値を固定して変化には気づけるようにした。正体は段 17 で場面を組むときに追う |
| `src/input/` と `src/render/camera.ts` に単体テストが 1 件もない。カメラを動かすと基準画像 42 枚が全部動くので原因が切り分けられない | 同上 | 未対応。段 37 で埋める |
| 揚力傾斜 4.0 の補正式が書かれておらず、値だけでは検算できなかった | 同上 | 式を書いた。`tests/tools/docsNumbers.test.ts` が式と実装の一致を見る |
| **プリセット表に列があるのに実装が読まない。**`quality.smaa` は node 経路が読まない（URL の `?smaa=0` しか見ない）。`?ta=0` は配線が無かった（段 20c で繋いだ）。`clampScale` は旧経路 0 対 node 1 でずれている。`CLAUDE.md` は「表に載らない設定項目を作らない」と逆向きだけを縛っている | 段 20c で `low` の落ちる列を二分していたとき、候補を読みながら気づいた | 未対応。プリセットの列と実装の読み手を突き合わせる検査が要る（`tests/tools/docsNumbers.test.ts` と同じ形で書ける見込み） |

## 測り方で踏んだもの

| 何が起きたか | 何で気づいたか | いま何が守っているか |
|---|---|---|
| `about:blank` で `navigator.gpu` を見て「WebGPU は使えない」と結論した。保安コンテキストではないため undefined になる | 同じ測定を localhost 由来のページでやり直した | `tools/webgpu-probe.mjs` が `secureContext` を必ず出す |
| adapter が取れることと描けることを同じものとして扱いかけた | 読み戻しまでやった | `tools/webgpu-probe.mjs` が画素をシェーダの式と突き合わせる |
| `toHaveScreenshot` が通ったことを「変わっていない」と読んだ。1 画素あたり 0.05 まで許すので、機体をまるごと差し替えても 10 枚中 4 枚が通る | Phase 4 | `tools/exact.mjs` が許容差なしで数える。描画に触る段ごとに走らせる |
| 合成ベンチ 1 本の比から所要を外挿しかけた。雲では +31% の見積りが実測 +58% だった。**段 9 では向きすら逆だった。**段 0 の合成ベンチは「WebGPU が 1.29 倍遅い」、実物の場面では WebGPU が 1.23 倍速い | Phase 3 の記録と、段 9 の実測 | 未対応。人が思い出すしかない。`docs/measuring.md` に見積りと実測を並べて残した |
| 21 条件を全部回すと統合 GPU が熱で遅くなる。ばらつきが 0.35 から 6.01 ms へ悪化した | Phase 6 | `?only=` で条件を絞る。`benchUnreadable` が読めない表を判定する |
| CI の E2E が 25 分の上限に当たった。テストは 1 件も落ちていない。2 分割のまま所要が 16.7 → 24.0 → 25.7 分と育っていたが、通り続けていたので余裕が減っていることが見えなかった | 上限に当たって初めて | 4 分割にして 1 台 13 分前後へ。上限を 18 分に下げ、余裕を 1.4 倍に留めた。**当たったら分割を増やす。上限は上げない** |
| `--shard` はテストの本数で割る。並びはファイル順・行順なので、末尾に固まったライブ UI が 1 台へ寄る。4 分割にしても最長 17.4 分で上限 18 分に対し余裕 1.03 倍だった | 上限に当たって初めて。8 分割にしたあとも最短 4.1 分・最長 10.5 分のまま開いていた（run 34056675131） | `PWTEST_SHARD_WEIGHTS` で台ごとの本数を所要から決める。最大テスト秒が 1,221 から 714 へ下がり、段の所要は 10.5 分から 6.3 分。重みは `tools/shard-weights.mjs` が CI のレポートから計算し、**自分の並びの読みを均等割りの再現で検算してから**出す。個数と台数の噛み合いは `tests/tools/shardWeights.test.ts` |
| 遅さの正体は競合だった。ページ読み込みは実測 3.4 秒、テストの中身も待ち 1.5 秒 × 2 程度。SwiftShader は CPU 律速なので、ワーカーが同時に描くと 1 件が 20〜45 秒へ伸びる | 段 5 で `preset` を振って測った。**所要が分け方で動くこと**は、重みを入れた run 34100372031 で 7 番のテスト秒が予測 714 に対し実測 891（+25%）になって分かった | 未対応。重い物を 1 台へ集めると、それらが同時に走って 1 本ずつが遅くなる。**釣り合わせるほど入力が動くので 1 周では決まらない。**重み（`tools/shard-weights.mjs`）と分割数で寄せ、見込みに +25% を乗せて余裕を取っているだけ |
| CI の E2E が実時間の待ちで落ちた。`waitForTimeout(3000)` のあと `speed > 50` が 30 秒待って 0 のまま。スロットルは `dt / 2.5` を積むので、シムが実時間に追いつかないと開き切らない。**リトライで通ったので隠れていた** | run 33378420162 のログに `✘` を探して | `tests/e2e/smoke.spec.ts` が値で待つ形になった。ポーズの復帰も同じ形へ直した |
| 浮動小数の除算で境目をちょうど 1 にしようとした。`min(col / 47.0, 1.0)` は逆数の乗算へ畳み込まれ、`47 * fl(1/47) = 0.99999994` になる。列 47 の 64 画素が早期打ち切りの枝を通らず、**枝を通していないまま「一致した」と読むところだった** | 枝ごとの画素数を先に数えた。1,024 と出た（期待 1,088） | 列の比較で 1 を作る。`tests/e2e/node-path.spec.ts` が枝の画素数を 1,088 と 3,008 で縛り、歯型 `overlay-probe-never-opaque` が片寄りを噛む |
| 早期打ち切りは値としては何も変えない。`overlay.a == 1` の画素は打ち切っても打ち切らなくても `base * 0 + overlay.rgb` になるので、**バイトを比べても枝を通ったかどうかは出てこない** | 段 13・14・16 で 3 度踏んだ形なので、移す前に枝の数え方を先に置いた | マーカーを焼いて画素数で数える。加えて `renderer.debug.getShaderAsync` で本文を読み、下地の組み立てが `else` の中にあることを位置で確かめる |
| **絵はパスの順を見なかった。**雲のマーチを場面のパスより先に走らせても、区画平均 16 個が 1 つも動かない。深度テクスチャに前のフレームの中身が残るので、同じ絵が出る。**1 フレーム前の深度で打ち切っていても気づけない** | 式の順を入れ替えて撮り直し、区画平均の差が 0.000000 のまま、描画呼び出しの数だけが 51 から 0 へ落ちた | `tests/e2e/node-path.spec.ts` が「雲を焼いた時点の描画呼び出しが 20 より多い」ことを見る。式の順を入れ替えると落ちる |
| `info.render.frameCalls` は全画面クアッドのパスも 1 つずつ数える。雲影と SMAA が増やすので、**場面が描かれたかどうかは出てこない。**順の見張りに使うと嘘をつく | 同上。順を入れ替えても `frameCalls` は 2 のまま同じだった | 順は描画呼び出しの数で見る。`frameCalls` はパスの数を数える用途に留める |
| **`compileAsync(scene, camera)` は場面の物しか組まない。**`RenderPipeline` の全画面クアッドも雲の 4 枚も場面に入っていないので、1 枚目にシェーダの生成が乗る。実測で 1 枚目 3,210.7 ms 対 2 枚目 987.0 ms | 段 19 で 1 枚目と 2 枚目を分けて測った | 雲は `compile()` で組む。鎖は暖機の 1 枚で組む。`tests/e2e/node-path.spec.ts` が「暖機のあとの 1 枚目が暖機の 0.6 倍未満」を見る |
| **壁時計の絶対値を並列の E2E で縛ると競合を測る。**起動の総和に「15 秒以内」を置いたら、単独 7,345.9 ms のものが spec を丸ごと回すと 31,702 ms で落ちた。SwiftShader は CPU 律速で、ワーカーが同時に描くと伸びる | 段 19 で検査を足した直後、単独では通るのに通し実行で落ちた | 検査は定常の 1 枚に対する比で縛る（実測 5.9 倍、上限 25 倍）。絶対値は直列の実測として `docs/measuring.md` に残す |
| **`trackTimestamp` は静かに false になる。**`device.features` に `timestamp-query` が無い環境では `{ trackTimestamp: true }` で作っても有効にならず、`resolveTimestampsAsync()` が `undefined` を返す。例外は出ないので「GPU 時間が 0」を不具合として追いかねない | 段 18 で node 経路の GPU 時間を取るとき、ADR 0010 の段 0 が先に記録していた | `undefined` は数を作らずに捨てる。**回収できた件数がそのまま「測れたか」**になり、`tests/e2e/node-path.spec.ts` が両経路で 4 件以上を要求する |
| **組み立ての時点で光に `castShadow` が立っていると、three の光の系が影のノードをもう 1 つ作る。**その本体は光の寄与が畳まれると生成されず、`shadowMap` が null のまま `updateBefore` だけが走って `compileAsync` の中で落ちる。`AtmosphereLight` でも明るさ 0 の指向光でも同じ | 段 17c で `shadow(light)` を地形へ入れたら `Cannot read properties of null (reading 'depthTexture')` が出た。自前で呼んだノードは `shadowMap` を持っていた（別のインスタンスが落ちていた） | 組み立てのあいだは `castShadow` を伏せ、`compileAsync` のあとで立てる。**伏せたままでは影マップが焼かれない**ので立てる側も要る。`tests/e2e/node-path.spec.ts` が投げ手を切った差を最大 10 階調で縛る |
| **大気の LUT ができる前に照度を焼くと 3 成分とも 0 が返る。**例外は出ないので、地表が真っ黒になった絵を「置き換えが効いていない」と読みかけた。`AtmosphereLUTNode.setup()` が走るのは `compileAsync` の中で、中身ができるのは `updateTextures` の後 | 段 17c で照度を焼いたら区画平均が 16 個とも 0.000 だった | 焼くのを `updateTextures` の後へ移した。`tests/e2e/node-path.spec.ts` が間接照度を 3 成分とも 0 より大きいことで縛る |
| **差分を場面の絵で測ると、対象が映っていないぶん小さく出る。**地表のライティングを置き換えて 736 バイト・最大 1 階調しか動かず、効いていないように見えた。プローブの構図では地表が画面をほとんど覆っていなかった | 矩形で密に測り直したら 12,288 バイト・最大 99 階調だった | 差分の帰属は対象が覆う場所で測る。`tests/e2e/node-path.spec.ts` が地表の 5 km 角で 1,000 バイト以上動くことを見る |
| GLSL の `out` 引数を TSL へそのまま写せない。枝を書き込む先を引数で渡しても閉包で渡しても**全部 0 のまま**返る。色を使わない呼び方をすると、色を返す `Fn` の呼び出しごと消えるため（既に記録のある「`Fn` の返り値を使わないと本体が生成されない」の別の顔） | 枝の画素数を両側で比べたら、GLSL が 2,591 に対して TSL が 0 だった | 出すものを JS の分岐で選ぶ（生成時に決まる）。`tests/e2e/node-path.spec.ts` が枝の画素数を両側で突き合わせる |
| **GLSL の予約語を識別子に使うとコンパイルが黙って落ちる。**three は `Shader Error` をコンソールへ出すだけで例外を投げず、無効なプログラムのまま `useProgram` を呼び続けるので、そのメッシュだけが消える。3 度踏んだ（`patch`、`half`、段 17b の `active`） | 段 17b で海面が消え、基準画像 38 枚が動いた。**どの行かは二分探索 4 往復とコンソールの全件出力でようやく出た**（最初は代表 4 件しか印字せず、`ReadPixels` の警告に埋もれていた） | `tests/render/shaderReserved.test.ts` が `src/` のシェーダ 13 本を予約語 39 個で走査する。注記の中は見ない。歯型 `shader-reserved-word` |
| 雲の材質の組み直しを 1 枚の絵で確かめようとして 2 度外した。`setSize` が履歴を捨てるぶんで 288 バイト、足し込みの状態を揃えても **ずらしがフレームごとに動くので 328 バイト**。足し込みが均すことを当てにしている値を 1 枚で比べていた | 同じプリセットを当て直しただけなのに差が消えなかった | 生成された本文の文字列で比べる。`tests/e2e/node-path.spec.ts` が「同じプリセットなら同じ本文」と「`cloudDetail` が逆なら違う本文」の両方を見る |
| **GPU 時間の解決は器が 1 つしかない。**WebGL2 のクエリは 1 本ずつ独立した物なので何本でも同時に飛ばせるが、`resolveTimestampsAsync()` はそうではない。決着前に重ねると three は解決を走らせず、WebGL は `lastValue`、WebGPU は走っている promise を返す。**解決を決着前に重ねると前の回の値が返る**のに札は今の回のものが付き、`lastValue` の初期値は 0 なので、0 を数に混ぜると最小値が 0 になる | E2E を 1 本だけ回すと 8 枚とも間に合って通り、265 本を並列で回すと `gpuFrameMs` が 0 で落ちた。競合に依存するので単独実行では出ない | 決着するまで次を投げない。0 と `undefined` は数を作らずに捨て、捨てた回数を `dropped` で数える。`tests/render/nodeTimer.test.ts` が擬似のレンダラで台本どおりに固定する |
| **釣り合わせると重い物が同じ台に集まり、テスト自身の待ちに当たる。**シャードを所要で分けた結果、最も重い 16 件が 1 台に集まって `RenderPipeline でポストの鎖が立つ` の 120 秒待ちを超えた。本番もリトライも落ちた | 余裕が 1.15 倍（104.6 秒）→ 1.03 倍（116.2 秒）→ 超過と減り続けていた。**通り続けていたので見えなかった。**この repo で 120 秒に当たるのは 2 度目（`harness.ts` の `openLive` が 1 度目） | プローブの待ちを 300 秒へ。**これは固まりの検出ではない**（それは e2e.yml の段の上限 18 分が担う。`playwright.config.ts` の注記どおり）。`test.describe.configure({ timeout })` は `mode` を渡さなければ `_parallelMode` を書かないので、群の作り方と `PWTEST_SHARD_WEIGHTS` の前提は崩れない |
| **断片のバイトが一致しても材質が一致しているとは限らない。**`?spriteprobe=1` は断片を全画面クアッドへ焼いてバイトを比べるだけで、合成の仕方も深度の書き込みも両面描画も一度も通らない。フレアの芯は深度を書き、暈は書かない。食い違うと「縁のはっきりした暗い円」が出る | 段 20a-1 で node 版の材質を書くときに気づいた。**プローブは材質を一度も作っていなかった** | `tests/render/radialSpriteFactory.test.ts` が両版の合成・深度・面・透明を突き合わせ、「不透明な芯だけが深度を書く」を既知の値でも確かめる（両方 false でも通る形を避ける）。歯型 `sprite-node-depth-write` と `sprite-node-blending` |
| **帳簿が uniform を直に書いていた。**`scene.ts` は雲影の中心・有効/無効、太陽の向き、太陽と天空の放射輝度、モーフの基準の 6 つを `terrainUniforms.*.value` へ書いていた。**node 材質はそこを読まない**（`NodeSurfaceState` が自分の `uniform()` で持つ）ので、node 経路では全部どこにも届かず初期値のまま固まる。**絵は出るので気づけない** | 段 20a-2-3 で `createNodePipeline` を書く前に読み手を数えたとき。書いてから気づくと、太陽の向きが違うのか移植が違うのかを切り分けられない | 書き先はパイプラインが知る（`ScenePipeline.setSurfaceFrame`）。読み手が 0 になった `ScenePipeline.terrainUniforms` は口ごと外したので、この経路は戻らない |
| **手で `render()` を回すと `FRAME` 型のノード更新が 1 度しか走らない。**`NodeFrame` が `frameId` で重複を潰し、その番号を進めるのは `Animation.js` の `nodeFrame.update()` だけ（`info.reset()` が同じ場所にあるのと同じ理由）。場面の `PassNode` も雲のパスも 2 枚目以降は飛ぶ。**絵は出続ける**（レンダーターゲットに前の中身が残る） | 段 20a-2-3 で `?gpu=3` を立てたとき。絵は平均輝度 163.9 で正しく出ていたのに、描画呼び出しが 2・三角形が 3 だった。1 枚目は 167 呼び出し・458,486 三角形 | `advanceNodeFrame(renderer)` を毎フレーム呼ぶ。**`nodeFrame.update()` は呼ばない**（`performance.now()` から `deltaTime` を進めるので実時間が描画へ入る）。`tests/render/nodeFrame.test.ts` が番号の進みと実時間の口を触らないことを縛り、`tests/e2e/node-path.spec.ts` の `本番の場面が node 経路で立つ` が最後のフレームの描画呼び出しを 100 より多いことで見る。歯型 `node-frame-no-advance` |
| **`renderer.shadowMap.enabled` を立てずに `shadow(light)` を呼ぶと本体が生成されない。**`TSL: Invalid generated code, expected a "float".` をコンソールへ出すだけで進み、影マップは null のまま。最初の描画が `updateShadow` の中で `Cannot read properties of null (reading 'depthTexture')` で落ちる。**例外の出どころが 2 段離れる** | 段 20a-2-3 で `createNodePipeline` を書いたとき。プローブには有効化があり、共有へ出していなかった | `configureNodeAircraftShadow`（`pipeline/nodeShadow.ts`）が有効化と箱の設定をまとめて持つ。プローブと本番の場面が同じ関数を通る |
| **壁時計の待ちは、遅い経路で空振りを作る。**`waitForTimeout(600)` は GLSL 経路の 1 フレーム 50 ms を前提にした値。node 経路は 800 ms 前後なので**1 枚も進まない。**進まなければ撃つ機会が無いので「設定を開いている間は撃っていない」が空振りで通る。**通っているのに守れていない** | 段 20a-3 で `開いている間は撃てない` の後半（閉じたら撃てる）が落ちて、前半を読み直したとき | フレームで待つ（`advanceFrames(page, 60)`。`hook.frame` は 120 Hz のシムの番号）。撃てたことは値で待つ。**GLSL 側の所要が 6 秒から 22.2 秒に伸びた**ので、元の 600 ms は GLSL 経路でも待ち足りていなかった。`smoke.spec.ts` には `waitForTimeout` が残り 8 か所あり、同じ危険を持つ（未対応） |
| **設定していないプロパティは `null` ではなく `undefined`。**`skyEnvironmentSize` が 0 のとき `scene.environmentNode` を設定しておらず、`!== null` の判定が true になった。low プリセットで「環境反射が切れている」が通らない | 段 20a-3 で node 経路の 183 件を回したとき | 0 のときも明示的に `null` を入れ、判定は真偽値で見る。影のほうは `aircraftShadowMapSize === 0` で立てず、`buildNodePipeline` へ投げ手を渡さない（渡すと `castShadow` が立つ） |
| **釣り合わせを繰り返すと悪くなる。**シャードの重みを実測から 2 周目を計算して当てたら、開きは 2.81 倍から 2.30 倍へ縮んだのに**最長の段が 424 秒から 456 秒へ伸びた。**上限に効くのは最長のほう。予測 536 テスト秒に対し実測 872（+63%）で、1 周目の +25% より大きく外れた | 1 周目の重みで 2 回とも最長 424 秒（同値）だったところに、2 周目を当てて 456 秒になった | 1 周目で止める。目的に置くのは最長の段の**実測**で、予測テスト秒ではない（釣り合った分配ほど楽観的に外れる）。悪化したら戻す。`docs/measuring.md` に 3 回の実測を並べた |
| **`exit code 0` は成功を意味しない。**基準画像の撮り直しが 3 回続けて 0 で終わり、1 枚も増えていなかった。原因は 3 つとも別（plan モードで背景の処理が止まった、`cd` が前のコマンドから残って別ディレクトリで「No tests found」、`toHaveScreenshot` の 5 秒超過）。ポートの占有も同じ形で、`4173 is already used` と出して 0 で終わる | 毎回ファイルの枚数を数えていたので 3 回とも捕まえた | 撮ったあとは枚数を数え、`npm run exact` で 2 回撮って一致を見る。`cd` は 1 つのコマンドの中で完結させる（並列に投げると共有シェルのカレントが流れる） |
| **比べる相手を間違えると、交換が無駄に見える。**node 経路の `compileAllPresets` を 4 段から 1 段へ減らすとき、比べたのは 4 段と `?precompile=0` だった。起動の programs がどちらも 67 だったので「43.7 秒を捨てるだけ」と読んだ。実際は 4 段 67 対 1 段 61 で、降格で増える数が 8 対 12。**34 秒の起動短縮と降格 4 個の交換だった** | 降格の閾値を 8 から 16 へ上げるときに、根拠を測っていないことに気づいた | 同じ harness で両方を測る（`docs/measuring.md` の段 20b）。片方だけ既存の測定から引いてこない |
| **並列化が効くかは内訳で決まる。**E2E の撮影は 1 ページが既に機械を使い切っていて、本数を増やしても 26% のばらつきに埋もれた（段 20a）。歯型は逆で、直列 186 秒のうち 149 秒が独立した vitest だったので**8 台で 42 秒（4.43 倍）まで割れた** | 段 20a で「並列化は効かない」と結論したあと、歯型で同じことを試す前に内訳を測った | 所要の内訳を先に見る。`tools/mutate.mjs` の注記に 4 条件の実測を並べた。`--workers` で振れる |
| **切り分けのフラグを部分一致で見ると条件が混ざる。**`BISECT.includes('size')` と `bisect.includes('csize')` を並べたので、`csize` が両方に当たって `applySize` と雲の `setSize` を同時に外していた。**「`setSize` を外しても落ちる」という読みが無効になった** | 別の実験（`?cloudScale=` で全段を同値にすると通る）と噛み合わなかったとき | フラグは前方一致か完全一致で見る。**片方が他方の部分文字列になる名前を選ばない** |
| **報告された症状を、報告された操作で再現するまで数値を読まない。**「傾けると雲も同じ動きをする」の報告に対し、飛ばさずにキャプチャで測り、次に飛ばしたら操縦を誤って墜落させ、**再現しないまま 3 回ぶん数字を出した。**最初の測定は空の帯（上から 40%）を見ていて、そこには雲も地形も入っていない。既定を戻したら再現しなくなり、**追従は描画ループが止まった後の画面**だと分かった | 報告者が旧経路の公開ページで確認した | 未対応。人が確かめるしかない。**測る領域に対象が入っているかを絵で見る**（`docs/lessons.md` の「差分の帰属は対象が覆う場所で測る」と同じ形） |
